apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rabbitmq-operator
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/budd9442/rabbitmq-platform.git'
    targetRevision: HEAD
    path: kubernetes/rabbitmq-operator
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: rabbitmq-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    createNamespace: true
