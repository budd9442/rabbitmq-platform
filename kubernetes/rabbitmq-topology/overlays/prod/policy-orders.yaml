apiVersion: rabbitmq.com/v1beta1
kind: Policy
metadata:
  name: orders-policy
  namespace: rabbitmq
spec:
  name: orders-policy
  pattern: "^orders-queue$"
  applyTo: queues
  definition:
    dead-letter-exchange: "orders-dlx"
    dead-letter-routing-key: "orders-dlq"
  rabbitmqClusterReference:
    name: rabbitmq-prod
